<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <TextView
        android:id="@+id/titleButtonThird"
        style="@style/HeadingSubTitle"
        android:layout_marginStart="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Email" />

    <TextView
        android:id="@+id/contentButtonThird"
        style="@style/HeadingSubTitle"
        android:layout_marginEnd="16dp"
        android:textColor="?attr/colorControlActivated"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="131.powerfull@gmail.com" />

    <com.google.android.material.divider.MaterialDivider
        android:id="@+id/dividerButtonThird"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/contentButtonThird"
        app:layout_constraintStart_toStartOf="@+id/titleButtonThird" />

</androidx.constraintlayout.widget.ConstraintLayout>
